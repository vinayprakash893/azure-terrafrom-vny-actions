name: input-steps-selection

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      cloud-tf:
        description: 'cloud or not'
        required: false
        default: false # Default branch name if not provided

permissions:
  id-token: write
  contents: read
  issues: write
  pull-requests: write

jobs:
  
  build:
    uses: vinayprakash893/azure-terraform-actions-reusable/.github/workflows/build-cloud-tf.yaml@main
    with:
      environment: dev
      terraform_directory: ""
      artifact-name: 'art-vny-1'
      cloud-tf: ${{ github.event.inputs.cloud-tf }}
    secrets: inherit
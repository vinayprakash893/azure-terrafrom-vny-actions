name: custom-utility
on:
  workflow_dispatch:
jobs:
  Run_utility:
    runs-on: ubuntu-latest
    name: Run
    steps:
      - uses: vinayprakash893/azure-terrafrom-vny-actions/actions/utility-master@main
      - name: echo
        run:  echo "hello"
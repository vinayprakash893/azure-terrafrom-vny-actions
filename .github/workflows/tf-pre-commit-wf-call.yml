name: tf-pre-commit-call

on:
  workflow_dispatch:


permissions:
  id-token: write
  contents: write
  issues: write
  pull-requests: write
  repository-projects: write


# jobs:
#   build:
#     uses: vinayprakash893/tf-precommit/.github/workflows/tf-precommit-reuse-wf.yml@main

jobs:
  build:
    uses: vinayprakash893/azure-terraform-actions-reusable/.github/workflows/build-pre-commit.yaml@main
    with:
      environment: dev
      terraform_directory: ""
      artifact-name: 'art-vny-1'
    secrets: inherit